# Zweitstimme Polling API Documentation

Comprehensive documentation for the Zweitstimme Polling API

## Local Development

To build and view the documentation locally:

```bash
git clone <repo-url>
cd polling-api-docs
uv sync
uv run mkdocs serve
```

The site will be available at `http://127.0.0.1:8000`
